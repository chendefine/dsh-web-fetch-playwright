name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        node-version: [22, 24]
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build

      # Tarball content check is platform-independent; /tmp + tar + grep
      # piping assumes a POSIX shell, so it stays on the Linux runners.
      - name: Verify package contents
        if: matrix.os == 'ubuntu-latest'
        run: pnpm pack --pack-destination /tmp/dsh-web-fetch-playwright-pack && tar -tzf /tmp/dsh-web-fetch-playwright-pack/*.tgz | grep -q '^package/lib/index.js$' || (echo 'lib/index.js missing from package' && exit 1)
